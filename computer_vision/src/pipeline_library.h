#include <stdio.h>
#include <list>
#include <iostream>
#include <opencv2/core.hpp>

namespace display {

    const std::list<std::string> views = {"Original", "HSV", "Mask", "Masked"};

    void generateWindows() {
        for (int i = 0; i < views.size(); i++) {

        }
    }


    
}