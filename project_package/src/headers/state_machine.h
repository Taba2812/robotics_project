#ifndef __STATE_MACHINE_H__
#define __STATE_MACHINE_H__

#include "ur5.h"

#pragma once

//we could implement an actual state machine to make sure every procedure is followed in the proper order and without clashing

class State {
public:
    //functions
};

#endif